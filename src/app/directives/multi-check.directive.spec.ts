import { MultiCheckDirective } from './multi-check.directive';

describe('MultiCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiCheckDirective();
    expect(directive).toBeTruthy();
  });
});
