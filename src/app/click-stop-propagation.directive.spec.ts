import { ClickStopPropagation } from './click-stop-propagation.directive';

describe('ClickStopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickStopPropagation();
    expect(directive).toBeTruthy();
  });
});
